services:
  resource-db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${RESOURCE_DB_NAME}
      POSTGRES_USER: ${RESOURCE_DB_USERNAME}
      POSTGRES_PASSWORD: ${RESOURCE_DB_PASSWORD}
    ports:
      - "${RESOURCE_DB_PORT}:5432"
    volumes:
      - ./init-scripts/resource-db/init.sql:/docker-entrypoint-initdb.d/init.sql

  song-db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${SONG_DB_NAME}
      POSTGRES_USER: ${SONG_DB_USERNAME}
      POSTGRES_PASSWORD: ${SONG_DB_PASSWORD}
    ports:
      - "${SONG_DB_PORT}:5432"
    volumes:
      - ./init-scripts/song-db/init.sql:/docker-entrypoint-initdb.d/init.sql

  eureka-server:
    build:
      context: ./eureka-server
    ports:
      - "${EUREKA_SERVER_PORT}:8761"

  resource-service:
    build:
      context: ./resource-service
    environment:
      SPRING_DATASOURCE_URL: ${RESOURCE_DB_URL}
      SPRING_DATASOURCE_USERNAME: ${RESOURCE_DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${RESOURCE_DB_PASSWORD}
      EUREKA_SERVER_URL: ${EUREKA_SERVER_URL}
    ports:
      - "${RESOURCE_SERVICE_PORT}:8083"
    depends_on:
      - resource-db
      - eureka-server
      - song-service1
      - song-service2

#  song-service:
#    build:
#      context: ./song-service
#    deploy:
#      replicas: 2
#    environment:
#      SPRING_DATASOURCE_URL: ${SONG_DB_URL}
#      SPRING_DATASOURCE_USERNAME: ${SONG_DB_USERNAME}
#      SPRING_DATASOURCE_PASSWORD: ${SONG_DB_PASSWORD}
#      EUREKA_SERVER_URL: ${EUREKA_SERVER_URL}
#    ports:
#      - "${SONG_SERVICE_PORTS}:8084"
#    depends_on:
#      - song-db
#      - eureka-server

# Uncomment the service above and remove the workaround services below after the commits
# https://github.com/gciavarrini/podman/commit/9dba4506d2b7325cc84427db1181d3df4d2fcda4 and
# https://github.com/gciavarrini/podman/commit/31221169adc6c76d293764be334619e0df79a0c2 from July 22, 2025
# mentioned in https://github.com/containers/podman/issues/15111 are released to Podman.

  song-service1:
    build:
      context: ./song-service
    environment:
      SPRING_DATASOURCE_URL: ${SONG_DB_URL}
      SPRING_DATASOURCE_USERNAME: ${SONG_DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${SONG_DB_PASSWORD}
      EUREKA_SERVER_URL: ${EUREKA_SERVER_URL}
    ports:
      - "8084:8084"
    depends_on:
      - song-db
      - eureka-server

  song-service2:
    build:
      context: ./song-service
    environment:
      SPRING_DATASOURCE_URL: ${SONG_DB_URL}
      SPRING_DATASOURCE_USERNAME: ${SONG_DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${SONG_DB_PASSWORD}
      EUREKA_SERVER_URL: ${EUREKA_SERVER_URL}
    ports:
      - "8085:8084"
    depends_on:
      - song-db
      - eureka-server
